import { Dishes } from "@prisma/client";

// const fetchCards = async (length: number) => {
//   for (let i = length; i < length + 25; i++) {
//     let dish: Dishes = await apiFetchCard(i);
//     likes.forEach((val) => {
//       if (dish.id === val.dish_id) dish.like = true;
//     });
//   }
//   await handleFetch();
// };
